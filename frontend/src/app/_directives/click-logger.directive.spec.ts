import { ClickLoggerDirective } from './click-logger.directive';

describe('ClickLoggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickLoggerDirective();
    expect(directive).toBeTruthy();
  });
});
